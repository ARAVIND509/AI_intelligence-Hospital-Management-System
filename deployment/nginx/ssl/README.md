# SSL Certificates Directory

Place your hospital domain's SSL certificates here:
- `server.crt` - SSL Certificate
- `server.key` - Private Key

For local hospital deployment, self-signed certificates or Let's Encrypt certificates can be placed here.
